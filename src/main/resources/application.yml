server:
  port: 8888


spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/spring-cloud-samples/config-repo
          clone-on-start: true
  security:
    user:
      name:
      password:
          #uri: ${USERPROFILE}\\Desktop\\config-server-repo